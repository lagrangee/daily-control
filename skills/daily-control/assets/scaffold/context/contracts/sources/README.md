# Source Contracts

Each concrete evidence source has one Markdown contract in this directory. Only files with `Enabled: yes` participate in refresh.

Use `/daily-control extend` to create, verify, and enable a Source Contract. Do not put credentials in this directory.
