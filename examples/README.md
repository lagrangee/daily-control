# Examples

These files illustrate links among Areas, Projects, Routines, and Daily records. They are synthetic and are not an alternative scaffold or runtime contract.

- [`context-root/`](context-root/) — a small snapshot after several conversations.

Use `/daily-control setup` to create a real Context Root from the installed Skill's scaffold.
