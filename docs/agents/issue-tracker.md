# Local Markdown issue tracker

Specs live at `.scratch/<feature>/spec.md`. Implementation tickets, when needed, live one per file under `.scratch/<feature>/issues/`.

Record triage near the top as `Status: needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, or `wontfix`.
